{
  "name": "swiggy-mern-clone",
  "version": "1.0.0",
  "description": "Full-stack Swiggy food ordering clone using MERN stack",
  "main": "server/server.js",
  "scripts": {
    "server": "node --watch server/server.js",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "keywords": ["mern", "swiggy", "food-delivery", "react", "mongodb", "express"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}

